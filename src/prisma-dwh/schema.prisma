generator client {
  provider        = "prisma-client-js"
  output          = "../../node_modules/@internal/prisma-dwh/client"
  previewFeatures = ["multiSchema"]
}

datasource db {
  provider = "sqlserver"
  url      = "sqlserver://msql-adwh-dev-southuk-01.database.windows.net:1433;database=sqldb-adwh-dev-southuk-01;user=SERVICE_PMTOOL;password={u9z1Vd8pc0q\\};encrypt=true"
  schemas  = ["operational", "transactional"]
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model project {
  order_id                 Int?
  customer_id              Int?
  customer_name            String?   @db.NVarChar(Max)
  sales_order_no           String    @unique
  project_no               String?   @db.NVarChar(Max)
  po_number                String?   @db.NVarChar(Max)
  resource_ecs             String?   @db.NVarChar(Max)
  resource_pcs             String?   @db.NVarChar(Max)
  supplier_contact         String?   @db.NVarChar(Max)
  amount_invoiced          Decimal?  @db.Decimal(18, 2)
  currency                 String?   @db.NVarChar(Max)
  original_value           Decimal?  @db.Decimal(18, 2)
  current_value            Decimal?  @db.Decimal(18, 2)
  quantity                 Decimal?  @db.Decimal(38, 5)
  quantity_allocated       Decimal?  @db.Decimal(38, 5)
  equipment_allocated      Decimal?  @db.Decimal(35, 2)
  sales_order_created_date DateTime?
  source                   String?   @db.NVarChar(Max)
  sales_order_name         String?   @db.NVarChar(Max)

  @@schema("operational")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model project_equipment {
  orderline_id             Int       @unique
  sales_order_no           String?   @db.NVarChar(Max)
  sales_order_name         String?   @db.NVarChar(Max)
  item                     String?   @db.NVarChar(Max)
  item_name                String?   @db.NVarChar(Max)
  quantity                 Decimal?  @db.Decimal(12, 5)
  unit_price               Decimal?  @db.Decimal(18, 5)
  line_item_price          Decimal?  @db.Decimal(18, 2)
  currency                 String?   @db.NVarChar(Max)
  quantity_allocated       Decimal?  @db.Decimal(15, 5)
  quantity_delivered       Decimal?  @db.Decimal(15, 5)
  quantity_invoiced        Decimal?  @db.Decimal(15, 5)
  equipment_allocated_date DateTime?
  status                   String?   @db.NVarChar(Max)
  source                   String?   @db.NVarChar(Max)

  @@schema("operational")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model labour_allocation {
  orderline_id       Int      @unique
  order_id           Int?
  sales_order_no     String?  @db.NVarChar(Max)
  item_code          String?  @db.NVarChar(Max)
  work_type          String?  @db.NVarChar(Max)
  quantity           Decimal? @db.Decimal(12, 5)
  quantity_allocated Decimal? @db.Decimal(15, 5)

  @@schema("operational")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model customer_list {
  customer_id   Int       @unique
  customer_code String?   @db.NVarChar(Max)
  customer_name String?   @db.NVarChar(Max)
  is_active     String?   @db.NVarChar(Max)
  updated_at    DateTime?
  source        String?   @db.NVarChar(Max)

  @@schema("operational")
}
